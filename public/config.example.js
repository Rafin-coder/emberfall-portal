// Modèle de configuration — COPIE ce fichier en "config.js" et remplis tes clés
// (ou laisse Netlify le générer via build-config.js + variables d'environnement).
window.GAMEPORTAL_CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: "",
  PORTAL_NAME: "EMBER ARCADE"
};
